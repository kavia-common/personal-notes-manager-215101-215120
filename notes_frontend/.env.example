# Example environment variables for Notes Frontend

# PUBLIC_INTERFACE
# Supabase URL for your project (https://<project-ref>.supabase.co)
REACT_APP_SUPABASE_URL=

# PUBLIC_INTERFACE
# Supabase API key (anon or service role as appropriate)
REACT_APP_SUPABASE_KEY=

# PUBLIC_INTERFACE
# Optional: override the table used for notes (defaults to 'notes')
REACT_APP_SUPABASE_TABLE=notes
